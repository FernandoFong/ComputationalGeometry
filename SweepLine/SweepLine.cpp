#include <bits/stdc++.h>
#define Point pair<int, int>
#define Segment pair<Point, Point>
#define p_a(s) s.first
#define p_b(s) s.second
#define X(p) p.first
#define Y(p) p.second

using namespace std;

/**
 * Given a set of segments, calculate the intersection points 
 * among them in O((n + k) log n) with k the number of intersections.
 */
vector<Point> sweep_line(vector<Segment> segments) {

}

int main() {

  return 0;
}
